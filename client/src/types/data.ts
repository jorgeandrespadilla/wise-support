export interface UserData {
    id: number;
    email: string;
    fullName: string;
    birthDate: string;
}

export interface UserInput {
    firstName: string;
    lastName: string;
    email: string;
    birthDate: string;
}