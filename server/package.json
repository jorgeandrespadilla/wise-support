{
  "name": "server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "nodemon --exec ts-node -r tsconfig-paths/register src/index.ts",
    "start:production": "cross-env NODE_ENV=production node -r tsconfig-paths/register build/index.js",
    "build": "rimraf build && tsc"
  },
  "dependencies": {
    "express": "^4.18.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.14",
    "@types/node": "^18.8.1",
    "cross-env": "^7.0.3",
    "nodemon": "^2.0.20",
    "rimraf": "^3.0.2",
    "ts-node": "^10.9.1",
    "tsconfig-paths": "^4.1.0",
    "typescript": "^4.8.4"
  }
}
